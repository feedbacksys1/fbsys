version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  scriptName: null
  command: gunicorn app.wsgi:application --bind 0.0.0.0:80 --timeout 120 --workers 2
  persistenceMount: /data
  containerPort: "80"
